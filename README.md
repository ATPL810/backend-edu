# backend-edu
Consists all the backend: servers, routers, middleware...

Configuration of the database name is availabe in the config folder in the file database.js, line 12

An environment file(.env) consists the mongodb_uri(can be altered to the user's customised mongodb_uri) and port number.
